function Mostrar()
{

	var numero;
	var seguir;
	var diferencia;
	var promedioPos;
	var promedioNeg;
	var acumuladorNeg;
	var acumuladorPos;
	var contadorPos;
	var contadorNeg;
	var acumuladorCero;
	var contadorPares;

	do
	{
		numero = parseInt( prompt("Ingrese un numero: "));

		if ( numero > 0 )
		{
			contadorPos++;
		}
		
		else if ( numero < 0 )
		{
			contadorNeg++;
		}

		else if ( numero + 1)		


		


		seguir = prompt("¿Desea continuar?");

	} while ( seguir == 'si' );






}//FIN DE LA FUNCIÓNi